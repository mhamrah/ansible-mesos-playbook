# mesos defaults file
# Generated by Ansible for {{ansible_fqdn}}

CLUSTER={{mesos_cluster_name}}
IP={{ansible_eth1.ipv4.address}}
LOGS={{mesos_log_location}}
ULIMIT="{{mesos_ulimit}}"
ZK="zk://{{groups.zookeeper_servers | join(':' ~ mesos_local_port }}/mesos"
MASTER="zk://{{groups.mesos_masters}} | join(':' ~ mesos_local_port }}/mesos"
OPTS="{{mesos_options}}"
