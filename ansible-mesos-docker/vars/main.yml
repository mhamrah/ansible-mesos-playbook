---
mesos_playbook_version: "0.1.1"

mesos_docker_lib_path: "/var/lib/mesos"
mesos_docker_executor_path: "{{mesos_docker_lib_path}}/executors"
mesos_docker_install_path: "{{mesos_docker_executor_path}}/docker"
mesos_docker_source_repo_name: "mesosphere/mesos-docker"
mesos_docker_branch: "master"
mesos_docker_binary: "bin/mesos-docker"
mesos_docker_raw_path: "https://raw.github.com/{{mesos_docker_source_repo_name}}/{{mesos_docker_branch}}/{{mesos_docker_binary}}"
